/**
 * SENTINEL ENGINE — Ghost
 * Privacy & DNS engine: DNS management, hosts file editing, telemetry control,
 * app permission management, privacy hardening.
 * Delegates to ghost-* IPC handlers in src/main/main.ts and src/main/ipc/dnsHandlers.ts
 */

export const ENGINE_NAME = 'Ghost';
export const ENGINE_VERSION = '5.0';
